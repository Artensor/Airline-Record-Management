document.addEventListener("DOMContentLoaded",() => {
  const headingsClients = ["ID", "Type", ];
  const headingsAirlines = [];
  const headingsFlights = [];

  const tabs = document.querySelectorAll(".tab")

  tabs.forEach(element => {
    
  
    if(headingsClients){

    }else if(headingsAirlines){

    }else{
    
    }

  });
})